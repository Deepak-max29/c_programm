#include<stdio.h>
int main(){
	int a;
	printf("enter a number : ");
	scanf("%d",&a);
	if (50>=a && a>=10)
	{printf("the no. is in range.");
	
	}
else{printf("the no.is not in range");
}
return 0;
}
